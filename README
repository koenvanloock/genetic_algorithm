================================

        GENETIC ALGORITHM

================================


to run pull the project
in intellij enable the SBT plugin

add a run configuration that executes the sbt task run

alternatively start via the command line with activator run
the application will be available at port 9000